<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Default: disallow cleartext (HTTP) to force HTTPS. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
      If you need to allow HTTP for a specific backend during testing,
      uncomment and set your domain below. Example:

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">your.backend.domain.com</domain>
    </domain-config>

    -->
</network-security-config>
