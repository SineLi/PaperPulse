# Minimal runtime dependencies for the backend (API + scheduler + fetchers + LLM).

# Core API
fastapi==0.128.0
uvicorn[standard]==0.40.0

# Auth / security
python-jose==3.5.0
passlib[bcrypt]==1.7.4
pytz==2025.2
email-validator==2.3.0
dotenv==0.9.9

# Scheduler
apscheduler==3.11.2

# Fetchers
requests==2.32.5
feedparser==6.0.12
lxml==6.0.2
python-dateutil==2.9.0.post0
playwright==1.57.0

# LLM summary
openai==2.15.0
jsonlines==4.0.0


# Postgres
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1